<% @daily_reports.each do |daily_report| %>
  <%= link_to daily_report_path(daily_report.id), class: 'text-dark  btn-default' do %>
    <%= daily_report.created_at.strftime('%Y/%m/%d %H:%M:%S') %>
  <% end %>
<% end %>